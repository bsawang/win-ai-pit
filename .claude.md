# Windows 避坑指南

AI 驱动的 Windows 开发/运维踩坑知识库。**代码仓库**，不含坑数据。

## 架构（双仓库 + 运行时）

| 组件 | 位置 | 角色 |
|---|---|---|
| **代码仓库** | `github.com/bsawang/win-ai-pit` | pyrite 源码 + 插件 + `kb.yaml`（schema 权威源，在包内 `pyrite_windows_pitfalls/kb.yaml`），`pip install` 只读 |
| **数据仓库** | `github.com/bsawang/win-ai-pit-data` | `pitfalls/*.md` 纯数据，`git clone`，**全局唯一可写副本**，仅多一个 git PR 写入口 |
| **运行时** | `~/.windows-pitfalls/` | clone 数据仓库 + `kb.yaml`（init 从包内写入）+ `data/`（SQLite 索引） |
| **全局配置** | `~/.pyrite/config.yaml` | `knowledge_bases[0].path` = `~/.windows-pitfalls`，`index_path` = `~/.windows-pitfalls/data/index` |

**分工**：控制层（schema/解析/迁移）全在代码仓库；数据仓库只是数据副本 + PR 写入口，不做任何适配。

## 数据流

```
查坑：search_pitfall → 本地索引(~/.windows-pitfalls/data/index) → 未命中 → git fetch → 有变化 pull+重建索引 → 重搜
记坑：record_pitfall → 写 ~/.windows-pitfalls/pitfalls/*.md → add/ 分支 → push → PR → auto-merge → 数据仓库 master
```

记录路径（add/ 分支 → PR → auto-merge）全在数据仓库完成，与代码仓库无关。

## 关键命令

```bash
bash ~/.claude/scripts/pitfalls.sh search '{"query":"关键词"}'     # 查坑（MCP → 数据仓库索引）
bash ~/.claude/scripts/pitfalls.sh record '{"title":"..","symptom":"..","root_cause":"..","solution":".."}'  # 记坑（自动 add/ 分支→PR→合入）
windows-pitfalls init      # 初始化：clone 数据仓库 + 写 kb.yaml + 建索引 + 配 MCP
windows-pitfalls start     # 启动 MCP Server（Claude Code 自动调用）
windows-pitfalls index     # 手动重建索引
windows-pitfalls log       # 查看 search/record 调用记录
```

**注意**：`pitfalls.sh` 需要 `export PYTHONUTF8=1`（已在脚本内），否则中文乱码。

---

# 调试速查（不翻代码定位问题）

## 1. record 后 PR 建错仓库 / 不生效

- **现象**：记坑后 PR 出现在 win-ai-pit（代码仓库）而非 win-ai-pit-data
- **定位**：`git -C ~/.windows-pitfalls remote get-url origin`
- **根因**：运行时 remote 仍指向旧代码仓库（迁移遗漏）
- **解法**：`git -C ~/.windows-pitfalls remote set-url origin https://github.com/bsawang/win-ai-pit-data.git`

## 2. 中文乱码 / 分支名乱码（GBK 编码）

- **现象**：search/record 输出乱码，或分支名 `add/20260807-Git-Bash-è°...`（GBK 字节）
- **定位**：`python -c "import sys; print(sys.stdout.encoding)"` → 应输出 `utf-8`
- **根因**：Python stdout/subprocess 默认 GBK。见 `pitfalls.sh` 顶部的 `export PYTHONUTF8=1`
- **解法**：确认 `pitfalls.sh` 第 13 行附近有 `export PYTHONUTF8=1`；手动跑 python 时加 `PYTHONUTF8=1`

## 3. 索引重建跳过某条坑（YAML frontmatter 解析失败）

- **现象**：`windows-pitfalls index` 报 `malformed frontmatter` / `mapping values are not allowed here`，条数比 .md 文件数少
- **定位**：报错指向 `line N`，就是该坑 frontmatter 的出错行
- **根因**：frontmatter 值里含英文冒号空格 `: `（如 `unknown encoding: cp936`），YAML 严格解析当嵌套 mapping
- **解法**：改全角冒号 `：`，或值加引号。全量检查：
  ```bash
  python -c "import glob,yaml; [print('FAIL',f) for f in glob.glob('pitfalls/**/*.md',recursive=True) if not _ok(f)]"
  ```
  记录时写入会自动加引号，手写的坑才容易踩

## 4. auto-merge 失败

- **现象**：数据仓库 PR 的 `verify-and-merge` 检查红
- **根因**（workflow 在数据仓库 `.github/workflows/auto-merge-pitfalls.yml`）：
  - PR 含**删除/修改**（不只新增）→ 检查「no deletions or modifications」失败
  - PR 改动 **pitfalls/ 之外**的文件（如 README、kb.yaml）→ 检查「only under pitfalls/」失败
- **解法**：数据 PR 只新增 `pitfalls/*.md`。改 schema/kb.yaml 是低频控制层变更，应手动 merge 不走 auto-merge

## 5. kb.yaml 缺失

- **现象**：索引/record 报 schema 相关错误，`~/.windows-pitfalls/kb.yaml` 不存在
- **根因**：kb.yaml 是运行时文件（init 从包内写入），数据仓库已 gitignore 它
- **解法**：从代码仓库包内复制 `cp E:\work\ai\win-ai-pit\pyrite_windows_pitfalls\kb.yaml ~/.windows-pitfalls/kb.yaml`，或重跑 `windows-pitfalls init`

## 6. 搜索多词/带符号不命中

- **现象**：搜 `MSYS_NO_PATHCONV`、`nltk cp936` 空结果，单关键词 `nltk` 正常
- **根因**：FTS5 tokenizer 对下划线/多词查询的处理限制（既有行为，非架构问题）
- **解法**：用核心单关键词搜（`MSYS`、`cp936`）

## 7. push 被拒 / master 分叉

- **现象**：`git push` 报 non-fast-forward，`[ahead 1, behind 1]`
- **根因**：本地和远程各有新提交（auto-merge 合并了别人的坑 / 本地漏 pull）
- **解法**：`git pull --rebase origin master && git push origin master`
- **注意**：`~/.windows-pitfalls` 的 `data/` 和 `kb.yaml` 已 gitignore，rebase 不受影响

## 8. 代码仓库与数据仓库混淆

- **现象**：在错误仓库里 git 操作（如在代码仓库 push 数据）
- **定位**：`git remote get-url origin`
- **规则**：
  - 代码仓库：只推代码/文档改动（master）
  - 数据仓库：只推坑数据（add/ 分支 → PR → auto-merge）
  - 运行时 `~/.windows-pitfalls`：clone 数据仓库，记坑/查坑都在这

---

## 设计文档

- `docs/design.md` — 设计决策（含「部署架构：代码与数据分离」章节）
- `docs/plan.md` — 开发计划
- 数据仓库使用说明：`win-ai-pit-data` 的 README
